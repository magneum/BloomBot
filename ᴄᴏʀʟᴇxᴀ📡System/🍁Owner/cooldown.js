// var type;
// var duration;


// if (Needs[1] === `s`) {
//   duration = Needs[0] * 1000;
//   type = `seconds`;
// } else if (Needs[1] === `m`) {
//   duration = Needs[0] * 60 * 1000;
//   type = `minutes`;
// } else if (Needs[1] === `h`) {
//   duration = Needs[0] * 60 * 60 * 1000;
//   type = `hours`;
// } else {
//   duration = Needs[0] * 60 * 1000;
// }
