{
  // These tasks will run in order when initializing your CodeSandbox project.
  "setupTasks": [
    {
      "name": "Install Dependencies",
      "command": "yarn install"
    }
  ],

  // These tasks can be run from CodeSandbox. Running one will open a log in the app.
  "tasks": {
    "npx forever --no-warnings νℓкуяє.js --color": {
      "name": "npx forever --no-warnings νℓкуяє.js --color",
      "command": "npx forever --no-warnings νℓкуяє.js --color",
      "runAtStart": true
    },
    "yarn install --ignore-engines": {
      "name": "yarn install --ignore-engines",
      "command": "yarn install --ignore-engines"
    },
    "npm install --save --force && clear && node νℓкуяє.js": {
      "name": "npm install --save --force && clear && node νℓкуяє.js",
      "command": "npm install --save --force && clear && node νℓкуяє.js"
    }
  }
}
